<%= turbo_frame_tag dom_id(@artifact) do %>
  <% if @artifact.video? %>
    <video controls width="960" preload="none" autoplay muted>
      <source src="<%= main_app.rails_blob_path(@artifact, disposition: :inline) %>" type="video/webm">
    </video>
  <% elsif @artifact.filename.to_s.end_with?(".zip") %>
    <% blob_path = main_app.rails_blob_path(@artifact, disposition: :inline, expires_in: 24.hours) %>
    <% blob_url = "#{request.base_url}#{blob_path}" %>
    <iframe src="/trace-viewer/index.html?trace=<%= CGI.escape(blob_url) %>" class="artifact__trace-viewer"></iframe>
  <% else %>
    <pre><%= @artifact.download %></pre>
  <% end %>
<% end %>
